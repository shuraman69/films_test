import React from "react";
import s from "./Nav.module.css"

const Nav = (props) => {
    return (
        <nav className={s.nav}>
                    <div></div>
        </nav>
    )
}

export default Nav;